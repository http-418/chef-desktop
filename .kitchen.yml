driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-14.04
  - name: debian-8.2

suites:
  - name: steam
    run_list:
      - recipe[desktop::steam]
  - name: wine
    run_list:
      - recipe[desktop::wine]
